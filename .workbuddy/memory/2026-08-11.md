# 2026-08-11

## yuki4266 GitHub 主页复刻 → WeatherCore

用户想把 GitHub 用户 `yuki4266` 的 profile README 复刻到自己的 `WeatherCore` 账号。

**用户参数**：
- GitHub 用户名：WeatherCore
- 城市：中国长沙芙蓉区（lat 28.20, lon 113.00, tz Asia/Shanghai）
- 技术栈 chips：保留原 5 行 AI 栈（LLMs/AGENTS/INFERENCE/RAG/LANG-OPS）不动
- banner/footer 设计：暂不改

**已完成替换**：
- `LICENSE` 第 3 行：Copyright (c) 2026 WeatherCore
- `bloom-header.svg` / `bloom-header-night.svg`：所有 yuki4266 → WeatherCore（包括 title 和 banner 大字）
- `tech-stack.svg` title：WeatherCore - tech garden
- `README.md`：4 处（alt 文本、streak-stats user、raw.githubusercontent 仓库路径、ghpvc username）
- `.github/workflows/sky.yml`：lat/lon/tz 改成长沙
- 保留不动：`yuki4266/living-scene@v1` 外部 action 引用、`snake.yml` 用 `${{ github.repository_owner }}` 自动适配、chips/*.svg 全部 38 个

**关键认知（这套主页图片的本质）**：
1. **手写 SVG 代码**（占 90% 视觉量）：bloom-header、garden-footer、chips/* 都是 path/circle + SMIL 动画，不是 AI 生成位图
2. **GitHub Actions 程序化生成**：sky.svg 每 3 小时调 living-scene action + Open-Meteo API 按天气重渲染；snake 用 Platane/snk 每 12 小时根据贡献图重生成
3. **外部服务动态图**：streak-stats.demolab.com、komarev.com/ghpvc 直接贴 URL，本地不留文件
4. **day/night 切换**用 `<picture>` + `<source media="(prefers-color-scheme: dark)">`，不是 JS

**待用户做**：
- 工作区不是 git 仓库，需 `git init` + 创建/复用 WeatherCore/WeatherCore 同名仓库 + push
- GitHub profile README 只在同名仓库（WeatherCore/WeatherCore）的 main 分支根目录 README.md 才会显示
- sky.svg 当前仍是 NC Durham 的 clear-summer 旧状态，push 后第一次 Actions 跑完会变成长沙实时天气
